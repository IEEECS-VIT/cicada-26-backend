const { Client } = require('pg'); require('dotenv').config(); const client = new Client({ connectionString: process.env.DATABASE_URL }); async function main() { await client.connect(); const sql = fs.readFileSync('supabase/migrations/00013_round_time_limits.sql', 'utf8'); await client.query(sql); console.log('Migration done'); await client.end(); } main().catch(console.error);